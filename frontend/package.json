{
  "name": "prayer-pattern-viewer-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:lessons": "node scripts/validate-lessons.mjs",
    "build:demo": "node scripts/validate-lessons.mjs && VITE_STATIC=1 vite build --base=/prayer-pattern-viewer/ --outDir dist --emptyOutDir"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "playwright": "^1.58.2",
    "vite": "^5.4.2"
  }
}
